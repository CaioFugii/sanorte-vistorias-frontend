/** Máximo de fotos por item do checklist (evidências vinculadas ao item). */
export const MAX_PHOTOS_PER_CHECKLIST_ITEM = 2;

/** Máximo de fotos gerais da vistoria (sem `inspectionItemId`). */
export const MAX_GENERAL_INSPECTION_PHOTOS = 5;
